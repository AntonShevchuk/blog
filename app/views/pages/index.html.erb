<% @pages.each do |page| %>
<div class="blog-post">
  <h4 class="blog-post-title"><%= link_to page.title, page_path(page) %></h4>
  <p class="blog-post-meta"><%= page.created_at %> by <%= link_to page.user.name, user_path(page.user) %></p>
</div>
<% end %>
